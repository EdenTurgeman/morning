{
  "name": "morning",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "//": "Node survives the web build's retirement for two jobs, both of which are tools rather than the app: ios/Tools/gen-seeds.mjs (no dependencies) and scripts/make-icons.mjs (sharp). scripts/verify-export.mjs is dependency-free too. There is nothing to build, serve or deploy here any more — the app is Swift.",
  "scripts": {
    "icons": "node scripts/make-icons.mjs"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}
